import numpy as np


def divide_and_conqueer(mat, L, H):


def greedy(mat, L, H):
    Lc = 0
    Hc = 0
    matc = np.zeros([1, 1], dtype=bool)
    matc = mat[0, 0]
    while Lc < L:
        g
        matc.
